% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modules.R
\name{module_available}
\alias{module_available}
\title{Check whether a Python module is available}
\usage{
module_available(
  module,
  action = c("none", "warning", "stop"),
  install_hint = NULL
)
}
\arguments{
\item{module}{A single Python module (import) name.}

\item{action}{What to do when the module is missing: \code{"none"} (the default,
just return \code{FALSE}), \code{"warning"}, or \code{"stop"}.}

\item{install_hint}{Optional character vector of extra lines appended to the
warning/error message, e.g. how to install the module.}
}
\value{
\code{TRUE} or \code{FALSE}, invisibly when \code{action != "none"}.
}
\description{
Tests whether a Python module can be imported, optionally warning
or erroring with an install hint when it cannot. Generalises the per-package
\verb{*_available()} checks (e.g. fafbseg's \code{dracopy_available()}), which become
thin wrappers that supply their own \code{install_hint}.
}
\examples{
\dontrun{
module_available("numpy")
module_available("DracoPy", action = "stop",
  install_hint = "Install with fafbseg::simple_python('basic').")
}
}
