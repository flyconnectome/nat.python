% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/int64.R
\name{pyids2bit64}
\alias{pyids2bit64}
\title{Convert a numpy 64-bit integer array to R}
\usage{
pyids2bit64(x, as_character = TRUE)
}
\arguments{
\item{x}{A numpy array, Python list or Python int of 64-bit integers.}

\item{as_character}{If \code{TRUE} (the default) return a character vector;
otherwise a \code{bit64::integer64} vector.}
}
\value{
A character or \code{bit64::integer64} vector.
}
\description{
Parses a numpy array (or Python list/int) of 64-bit integer ids
into a \code{bit64::integer64} vector or character, round-tripping through raw
bytes so no precision is lost.
}
\details{
Only \code{int64} and \code{uint64} numpy dtypes are accepted; a \code{uint64} array
is checked for values that would overflow signed int64 and errors if any is
found. The bytes are written to a temporary file and read back as doubles
whose class is then set to \code{integer64}, which is exact for all 64-bit ids.
}
\examples{
\dontrun{
np <- reticulate::import("numpy", convert = FALSE)
pyids2bit64(np$array(c("123", "456"), dtype = "uint64"))
}
}
\seealso{
\code{\link[=rids2pyint]{rids2pyint()}} for the reverse direction.
}
